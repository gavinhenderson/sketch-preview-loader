# Sketch Preview Loader - Example

This is a minimal example using the `sketch-preview-loader` to load a sketch preview into a react project

## Instructions

Install deps:

```
npm i
```

Run build:

```
npm start
```
